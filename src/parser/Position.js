/**
 * Position object.
 */
export default class Position
{
    /**
     * Cell Position
     * @param {number} column - Cell column.
     * @param {numver} row - Cell row.
     */
    constructor(column, row)
    {
        this.column = column;
        this.row = row;
    }
}
